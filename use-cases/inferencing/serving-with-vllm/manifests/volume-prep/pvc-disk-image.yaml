apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: block-pvc-model
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 100Gi
  storageClassName: standard-rwo